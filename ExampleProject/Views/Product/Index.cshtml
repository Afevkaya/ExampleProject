@* @model List<ExampleProject.Models.Product>

<ul>
    @foreach(var product in Model)
    {
        <li>@product.Name</li>
    }
</ul> *@

@* <h3>View Bag Gösterim (dynamic yapı)</h3>
<ul>
    @foreach(var product in ViewBag.products as List<ExampleProject.Models.Product>)
    {
        <li>@product.Name</li>
    }
</ul>

<h3>View Data Gösterim (boxing-unboxing yapı)</h3>
<ul>
    @foreach(var product in ViewData["products"] as List<ExampleProject.Models.Product>)
    {
        <li>@product.Name</li>
    }
</ul>


<h3>Temp Data Gösterim (boxing-unboxing yapı)</h3>
<ul>
    @foreach(var product in TempData["products"] as List<ExampleProject.Models.Product>)
    {
        <li>@product.Name</li>
    }
</ul> *@